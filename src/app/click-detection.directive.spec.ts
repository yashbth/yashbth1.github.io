import { ClickDetectionDirective } from './click-detection.directive';

describe('ClickDetectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickDetectionDirective();
    expect(directive).toBeTruthy();
  });
});
